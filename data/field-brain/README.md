# Field brain snapshots (GitHub)

Manifests synced from Hostess7 field drive for zero-cost panel reads when TEAM NVMe is not mounted.

- `manifest.json` — library catalog from `brain/library/`
- `context.json` — Hostess7 superintelligence arc
- `field_fingerprint.json` — micro-sig for unchanged detection
- `index.json` — brain root index

Refresh: `./scripts/sync-field-brain-github.sh`