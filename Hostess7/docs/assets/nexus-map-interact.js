/** NEXUS map interact — Pages-safe stub; full lane on loopback panel. */
(function (global) {
  "use strict";
  global.NexusMapInteract = global.NexusMapInteract || {
    ready: true,
    pages: true,
    wire: function () {},
  };
})(typeof window !== "undefined" ? window : globalThis);