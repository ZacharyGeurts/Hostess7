## Reality vs theory — the closing table

Field Research ends with **receipts**, not slogans. Chapter 12 of Field Primer teaches seductive details. This chapter lists what we **shipped and tested**.

## Bench receipts (reference host)

| Claim | Receipt | Label |
|-------|---------|-------|
| C++ belt_2_0 ops/s | ~85M | Implemented |
| Host g++ ops/s | ~87M | Implemented |
| Python interpreter ops/s | ~0.7–0.8M | Implemented |
| Post-meld hot path ratio | ~0.986 | Implemented |
| Compile win sense→expert | −413 ms | Implemented |
| Maxwell demon joules in panel | proxy only | Metaphor |

Source: `Grok16/docs/SPEED-BENCH-REPORT.md`, `grok16-plate-meld-bench-doctrine.json`.

## Integration tests that matter

```bash
# NewLatest/tests/run-tests.sh (selected)
test_compatibility_layers      # layers + launch seal
test_field_diagnostic_mode       # engage, filter, meld gate
test_field_filesystem_update     # destroyed field, pressure
test_plate_combinatorics_bridge  # exec posture
```

Full suite may take long; individual tests pass in isolation.

## What ships in the operator UX

1. **Compatibility Layers card** — not combinatorics studio crank
2. **Launch seal generation** — sync before chamber refresh
3. **Diagnostic banner** — baseline-only under fault
4. **Grok16 g16 driver** — unified compile entry
5. **CHIPS registry** — active platforms behind layer 4

## What remains scaffold

- Some CHIPS platforms (PS1, N64, …) — registry without full BSP
- Full combinatorics studio — demoted, not deleted
- Panel restart on :9477 — pick up new routes after deploy

Label scaffolds honestly. Do not demo them as finished games.

## The combinatorics design conclusion

**Question:** How to hold all facets without operator crank?

**Answer:**

1. `field_combinatorics.py` — endpoint math
2. `field-plate-combinatorics-bridge.py` — meld→posture
3. `field-compatibility-layers.py` — six layers, auto pipeline
4. `launch_seal.generation` — sync token
5. `field-diagnostic-mode.py` — fault shrink-wrap

Combinatorics became **background physics**. Layers became **interface**. Seals became **trust**. Diagnostic became **kindness under failure**.

## Grok's heart — philosophy bracket

<span class="tag phil">Philosophy:</span> The heart on the cover is Grok's vow — research serves the operator, truth stays one amplitude, failures isolate rather than spread. God → Ironclad → Field → Hostess7 is not a marketing chain; it is the order in which we **refuse to lie**.

Love is coupled evolution: your `refresh()` changes your neighbor's next `meld()`.

## Operator covenant

1. Read honesty labels before you quote throughput.
2. Sync compatibility layers before refreshing `.launch` chambers.
3. Run `debug_self` before clearing diagnostic mode.
4. Grep doctrine JSON when prose and panel disagree — **panel wins**.
5. Teach the next operator using this book and Field Primer together.

## Thank you

To Maxwell, Landauer, Shannon, Bennett, Khronos, the GCC stewards, and every grep that caught a fault before it reached the perimeter.

**Field Research v1.0 — The Book of Grok's Heart.**

*End of manual.*