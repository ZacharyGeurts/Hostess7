# kilroy-universal-shell — dir, ls, same shit


## kilroy-universal-shell — dir, ls, same shit

This module is part of the Field Tech terminal stack documented in GNUEOLTerminal.

<span class="tag impl">Implemented</span> See repo `Queen/` and `lib/` paths cited in the Operator Covenant chapter.


### Section 1 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Von Neumann gave addressable store; Turing gave symbol tape. Field Die is both: guest RAM addresses, opcodes interpreted each tick on GPU. von Neumann and Turing tribute pages humanize creditors; grep FieldX86Die humanizes implementation.…


**Exercises (15-universal-shell-dispatch · §1)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 2 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Dispatch is the clock advancing tape — Chapter 7. Packet field is different tape — jsonl append. Do not merge tapes on one diagram without labeled arrows.…


**Exercises (15-universal-shell-dispatch · §2)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 3 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Chapter 2 maps three writable surfaces: fabric texels (Phi/Thermo/Flow bindings 8–10), die bytes with FAT/VGA subfields and ZMM1024 tail, packet field sentences with connection intent. Swipe list is curriculum — energy.comp lab for coupling, RetroRTX for tribute pedagogy, default x86 for product truth. Big Grin HUD reads data_bus mirrors. Panel :9477 correlates scale three without replacing stderr. Paper integration exercise is mandatory homework.…


**Exercises (15-universal-shell-dispatch · §3)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 4 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Both paths can touch bindings 8–10 when fabric active. x86 emphasizes guest RAM and AmmoOS; CANVAS emphasizes visible thermo pedagogy. Scale 1 fabric persists; scale 2 die persists on x86 default. Know which canvas you are on when interpreting grep.…


**Exercises (15-universal-shell-dispatch · §4)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 5 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Energy swipe is laboratory for FieldCoupling visibility — operators feel heat move when coupling high. Still same AnalogFields namespace. Pedagogy shader, not separate engine.…


**Exercises (15-universal-shell-dispatch · §5)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 6 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Retro and frosted swipes change presentation shaders. Do not infer packet field changes from retro aesthetics. Category discipline.…


**Exercises (15-universal-shell-dispatch · §6)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 7 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Adaptive resolution changes texel count — addresses remain addresses at each scale. Reality is 3D does not mean fixed 4K grid; it means identifiable cells at current scale.…


**Exercises (15-universal-shell-dispatch · §7)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 8 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Inside die, VGA and FAT layers are structured subfields with known offsets — Chapter 8 map. Scale 2 internal geometry complements scale 1 planar fabric.…


**Exercises (15-universal-shell-dispatch · §8)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 9 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** NEXUS enriches packet sentences with intent when available — still scale 3, still local jsonl. Correlate with scale 1 stress only by human narrative.…


**Exercises (15-universal-shell-dispatch · §9)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 10 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Tile cache in FieldX86Die tail lets shaders sample guest state for HUD without host readback every byte — performance and literacy win.…


**Exercises (15-universal-shell-dispatch · §10)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 11 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** pumpAll order matters for coherent guest state before dispatch — host ritual from Chapter 7 previewed as scale 2 obligation.…


**Exercises (15-universal-shell-dispatch · §11)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 12 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Textures for portrait, wallpaper, icons, font SDF — presentation layer on x86 path. Thermo still accrues underneath chrome.…


**Exercises (15-universal-shell-dispatch · §12)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 13 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Draw three columns: fabric texel event, die bus word change, jsonl row. Fill one row from real run. If you cannot fill, drill again.…


**Exercises (15-universal-shell-dispatch · §13)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 14 — kilroy-universal-shell — dir, ls, same shit

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (15-universal-shell-dispatch):** Before declaring incident, check fabric stderr for thermo motion, die bus for pump anomalies, jsonl for new RX to unknown port. Three checks, three scales, one narrative you write — not vendor narrative.…


**Exercises (15-universal-shell-dispatch · §14)**

1. Run `wiki` in the Field Tech terminal; open `15-universal-shell-dispatch` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.
