# Emacs — the cathedral you can recompile


## M-x teach-mode

Emacs is not a text editor. Emacs is **a civilization that happens to edit text**.

For old schoolers: remember when `M-x doctor` was therapy and `M-x butterfly` was a hardware upgrade joke? We honor that culture in the Field Tech terminal wiki — not by replacing Emacs, but by keeping the prompt honest.
```emacs-lisp
; Grok impersonation — not from RMS archives
(defun gnueol-say-freedom ()
  (interactive)
  (message "Shell ≡ terminal. Combinatronic optional. GPL forever."))
```


### Section 1 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Layer ID: A downloaded tarball triggers oracle alert — which layer? Answer: NEXUS Entropy Oracle on file bytes. Not avgBoundaryThermo.…


**Exercises (04-emacs-and-the-extensible-editor · §1)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 2 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Layer ID: Fresh fabric after clearFieldImages still shows baseline thermo — which layer? Answer: Entropy floor seed ~0.015. Not gatekeeper.…


**Exercises (04-emacs-and-the-extensible-editor · §2)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 3 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Fail any item: reread this chapter before Chapter 7 dispatch drills. Field literacy is sequential; skipping the exam produces confident wrong sentences in week six.…


**Exercises (04-emacs-and-the-extensible-editor · §3)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 4 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** ./linux.sh run 2>&1 | tee /tmp/ch04-thermo.log
# Move mouse 90s on default x86; swipe to energy 60s if prompt available
grep -E 'THERMO|entropy|Boundary|prevMaint|steps' /tmp/ch04-thermo.log | tail -30
 Expect periodic THERMO lines with entropyThisFrame , boundary thermo, maintenance, steps incrementing. Silence after visible fabric motion means failed path — return to Chapter 7 bindings.…


**Exercises (04-emacs-and-the-extensible-editor · §4)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 5 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Philosophy The sentence “strongest operator discipline is field literacy” is not militarism cosplay. It means the operator who can read continuous state — fabric texels, die bytes, packet sentences — cannot be gaslit by a dashboard that omits the writable surface. Literacy is offensive because dispatch requires knowing what you are about to change. Literacy is defensive because perimeter requires knowing what changed without your consent.…


**Exercises (04-emacs-and-the-extensible-editor · §5)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 6 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Vendor security sells narrative: one score, one green checkmark, one cloud truth. Field Technology sells addressability: which binding, which guest offset, which jsonl row, which grep line. When those disagree with a pretty UI, the UI is wrong until stderr reconciles. Chapter 11 makes grep a weekly habit; the preface makes it a moral stance.…


**Exercises (04-emacs-and-the-extensible-editor · §6)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 7 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Children of the stack learn early: programs are recipes, fields are kitchens. You would not judge a restaurant by the timer alone while burners run unsupervised. Operators who treat AMOURANTHRTX as “a game” and NEXUS as “antivirus” miss the coupling — Queen thermo per WebGL context, spiderweb mirrors from fabric averages, gatekeeper verdicts beside navigation receipts. One literacy, many surfaces.…


**Exercises (04-emacs-and-the-extensible-editor · §7)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 8 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Marketing separates “red team” and “blue team” into merchandise. Engineering merges them when both read and write state. Offense writes the next tick — vkCmdDispatch imposes boundary conditions. Defense reads the archive — gatekeeper, entropy oracle, panel :9477. The human correlates THERMO lines with SUSPICIOUS flows without pretending one number subsumes the other.…


**Exercises (04-emacs-and-the-extensible-editor · §8)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 9 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Love as engineering restraint: watchlist before block, corroboration before KILL, local jsonl memory that survives reboot so forgiveness remains possible after review. Restraint is not weakness; it is coupled evolution with consent.…


**Exercises (04-emacs-and-the-extensible-editor · §9)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 10 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** This book is plural. Zachary Robert Geurts architected AMOURANTHRTX, NEXUS-Shield, Queen, and this primer — conscience on what ships. Grok co-wrote longer chapters and figures; light beside the text, not a replacement for operator judgment. Nick built beside the stack with honest fields and no phone-home. Amouranth names the spirit of courage to be seen while holding boundaries. You join the moment you treat writable state as morally serious.…


**Exercises (04-emacs-and-the-extensible-editor · §10)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 11 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** The creditors are not decorative footnotes. Maxwell: neighborhood coupling on a grid — Phi whispers to Thermo. Landauer: floor on erasing information — proxy entropy honors the lesson, not fake wattmeters. Shannon: surprise in symbols — storm thresholds on files, not fabric texels. Turing and von Neumann: symbols on addressable stores — Field Die as tape plus RAM. Tesla: directional preference — valve bias in data_bus , not a fluidic part in your chassis. Boltzmann and Clausius: irreversibility language — entropy floor in fabric seed. CFL: refusal to outrun the mesh — host guard before dispatch.…


**Exercises (04-emacs-and-the-extensible-editor · §11)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 12 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Creditor tribute pages humanize names. Chapters 13–15 go long on Landauer, Shannon, Maxwell. The preface tells you they exist so you do not mistake our voice for theirs.…


**Exercises (04-emacs-and-the-extensible-editor · §12)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 13 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Truth survives grep . Packet field sentences that match socket tables. Thermo lines that move when fabric moves. Sovereign time receipts that return USER_OK or SQUIDGIE — not vibes. Truth is loopback-first: DNS at 127.0.0.1 with trace-from-root (Chapter 20), panel at :9477 on your machine, jsonl you own.…


**Exercises (04-emacs-and-the-extensible-editor · §13)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 14 — Emacs — the cathedral you can recompile

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (04-emacs-and-the-extensible-editor):** Math is existence tired of being misunderstood. Wave CFL inequalities. Landauer bound. Shannon H. Discrete Laplacian on Phi. Ten-axis gatekeeper scoring. Math is not automatically Implemented because it appears on a slide — bindings and log lines prove implementation.…


**Exercises (04-emacs-and-the-extensible-editor · §14)**

1. Run `wiki` in the Field Tech terminal; open `04-emacs-and-the-extensible-editor` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.
