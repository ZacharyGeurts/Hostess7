# Command reference — Field Tech terminal


## Commands

`help` · `wiki` · `field-tech` · `kernel` · `combinatorics` · `clear` · `cd` · universal aliases.


### Section 1 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Explicit time stepping updates each texel from neighbor information that arrived at time t. If information travels farther than one cell per step — wave speed × Δt > Δx — the scheme invents neighbors that have not spoken yet. That is NaN theology. Courant, Friedrichs, and Lewy named the inequality operators inherit as CFL condition .…


**Exercises (29-command-reference · §1)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 2 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Wave channel intuition: disturbance crosses grid at speed c. One step advances Δt. Distance traveled c·Δt must not exceed cell spacing Δx — else dependency graph lies.…


**Exercises (29-command-reference · §2)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 3 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Thermo channel intuition: diffusion spreads heat wider each step — parabolic scaling. Distance grows like √(α·Δt), leading to:…


**Exercises (29-command-reference · §3)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 4 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Chapter 9 owns constants; Chapter 7 owns placement — CFL runs before vkCmdDispatch every frame. Intuition operators need: finer mesh (smaller Δx) tightens allowed Δt or forces lower c and α. Adaptive resolution (Chapter 2) therefore interacts with thermo headroom — not vanity scaling.…


**Exercises (29-command-reference · §4)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 5 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Options::AnalogFields::WaveSpeed maps to FCC slot data_bus[18] on x86 path. Host enforces hard caps (e.g. waveSpeed ∈ [0.01, 2.0] , dT ≤ 0.033 ) before fabric sees operator enthusiasm. Caps are not punishment — they preserve fabric existence for next tick.…


**Exercises (29-command-reference · §5)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 6 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** set AnalogFields.WaveSpeed 2.5
set AnalogFields.TimeScale 2.0
list AnalogFields
grep -i 'CFL\|clamp\|wave' run.log | tail -15
 Expect host to pull effective wave speed below operator request — list readback shows scaled truth. Screenshot of pretty waves without this grep is incomplete testimony.…


**Exercises (29-command-reference · §6)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 7 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** avgBoundaryThermo in ThermoAccountant (binding 2, mirror data_bus[25] ) summarizes mean boundary temperature / entropy density — where fabric meets edges and probes inject. Mouse movement is boundary offense: InjectStrength writes energy at probe positions carried through FieldSocket push constants.…


**Exercises (29-command-reference · §7)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 8 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Rising boundary thermo with flat interior averages tells a story: perimeter work without bulk equilibration — common in high inject sessions. Flat boundary with rising interior suggests diffusion catching up — different narrative, same grep discipline.…


**Exercises (29-command-reference · §8)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 9 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Chapter 4 names full ThermoAccountant fields; Chapter 3 teaches you to move mouse and expect boundary line to speak in THERMO stderr. Die-default x86 hides heatmap — boundary thermo still moves if coupling alive; enable debug HUD or trust log.…


**Exercises (29-command-reference · §9)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 10 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Default execution path is GPU interpretation via x86.comp — fat GPU, thin host. Optional ControlHostCpu enables FieldX86Emu assist; host cycles last frame can add terms to entropyThisFrame through FieldX86Emu::hostCyclesLastFrame() . Both paths remain proxy — not junction calorimetry.…


**Exercises (29-command-reference · §10)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 11 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Category error: claiming nvidia-smi power draw equals entropyThisFrame . Category error: disabling GPU path to “save thermo” while claiming field evolution — offense requires dispatch.…


**Exercises (29-command-reference · §11)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 12 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** Flowers and GreenWaves swipes emphasize organic flow and interference patterns on Classic canvas — curriculum shaders where thermo and Phi coupling is visible in ways Big Grin chrome suppresses. Week-one pattern: afternoon on Flowers with inject lab; return to die default for week-two operations.…


**Exercises (29-command-reference · §12)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 13 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** ThermoAccountant populates every swipe — canvas-agnostic obligation from Chapter 7. Flowers beauty does not excuse skipping THERMO grep.…


**Exercises (29-command-reference · §13)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 14 — Command reference — Field Tech terminal

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (29-command-reference):** # Swipe Flowers or energy; baseline 120s log segment
set AnalogFields.FieldCoupling 0.1
set AnalogFields.GateFidelity 0.3
set AnalogFields.EntropyFloor 0.01
# Record THERMO tail; then sweep:
set AnalogFields.FieldCoupling 0.9
set AnalogFields.GateFidelity 0.85
set AnalogFields.InjectStrength 4.0
grep -E 'THERMO|entropy|Boundary|prevMaint' run.log | tail -30
 Journal template per sweep row:…


**Exercises (29-command-reference · §14)**

1. Run `wiki` in the Field Tech terminal; open `29-command-reference` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.
