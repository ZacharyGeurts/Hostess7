# queen-terminal.py — allowlist, dispatch, combinatronic bridge


## Architecture

The Python backend is the **conscience** of the terminal. JavaScript paints ANSI; Python decides what may run.
```python
def dispatch_terminal(body):
    cmd = body.get('command', '').strip()
    comb = _combinatronic_dispatch(cmd, cwd)
    if comb is not None:
        return comb
    ok, reason = _command_allowed(run_cmd)
    ...
```


### Section 1 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Storyboard discipline trains operators to narrate dispatch without mysticism. Each frame is receipt. Long sessions are novels of receipts — grep tail is last paragraph, not whole book.…


**Exercises (11-queen-terminal-py · §1)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 2 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Knobs alter storyboard: high FieldCoupling makes chapters bleed together — Phi motion immediately warms Thermo; low coupling isolates chapters for pedagogy. High InjectStrength makes mouse protagonist; high GateFidelity makes gates snap digital; low fidelity keeps analog softness. PropalacticScale adds large wavelength weather on Phi — cosmic naming, local effect.…


**Exercises (11-queen-terminal-py · §2)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 3 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Thermodynamics laboratory ethics: always note starting knob state in lab journal; always restore defaults after session; always label proxy when sharing screenshots externally. Science reputation is honesty reputation.…


**Exercises (11-queen-terminal-py · §3)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 4 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Frame proxy entropy rises when storyboard work rises — Chapter 4 names layers. Fabric floor ensures Thermo never pretends zero — Chapter 4 second law. File oracle unrelated to frame storyboard unless you conflate — Chapter 4 forbids. Chapter 3 moves energy; Chapter 4 records irreversibility receipts. Read order sacred.…


**Exercises (11-queen-terminal-py · §4)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 5 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Every frame destroys information — diffusion, probes, maintenance, optional host assist. AMOURANTHRTX tracks that cost as accounting with honest labels, not as a laboratory calorimeter pretending to be nvidia-smi . Chapter 3 teaches Energy can be moved on the analog fabric: Phi whispers to Thermo through coupling; Flow carries gradients; CFL guards refuse numerical arson. This is Maxwell’s neighborhood on a grid — Chapter 15 goes long; here you learn to move knobs and read receipts.…


**Exercises (11-queen-terminal-py · §5)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 6 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Prior: Chapter 2 — Three Scales . Next: Chapter 4 — Entropy Receipts . Dispatch placement: Chapter 7 . CFL deep dive: Chapter 9 . Creditor: Clausius .…


**Exercises (11-queen-terminal-py · §6)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 7 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** newPhi ∈ [-2.0, 2.0] · newThermo ∈ [0.0, 1.5] · newFlow ∈ [0.0, 1.0]
 Clamps are numerical embodiment of kitchen realism — the shader refuses infinite temperature because the UI got excited. Host CFL guard and shader clamps are coupled defenses (Chapter 9).…


**Exercises (11-queen-terminal-py · §7)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 8 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Explicit time stepping updates each texel from neighbor information that arrived at time t. If information travels farther than one cell per step — wave speed × Δt > Δx — the scheme invents neighbors that have not spoken yet. That is NaN theology. Courant, Friedrichs, and Lewy named the inequality operators inherit as CFL condition .…


**Exercises (11-queen-terminal-py · §8)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 9 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Wave channel intuition: disturbance crosses grid at speed c. One step advances Δt. Distance traveled c·Δt must not exceed cell spacing Δx — else dependency graph lies.…


**Exercises (11-queen-terminal-py · §9)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 10 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Thermo channel intuition: diffusion spreads heat wider each step — parabolic scaling. Distance grows like √(α·Δt), leading to:…


**Exercises (11-queen-terminal-py · §10)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 11 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Chapter 9 owns constants; Chapter 7 owns placement — CFL runs before vkCmdDispatch every frame. Intuition operators need: finer mesh (smaller Δx) tightens allowed Δt or forces lower c and α. Adaptive resolution (Chapter 2) therefore interacts with thermo headroom — not vanity scaling.…


**Exercises (11-queen-terminal-py · §11)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 12 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** Options::AnalogFields::WaveSpeed maps to FCC slot data_bus[18] on x86 path. Host enforces hard caps (e.g. waveSpeed ∈ [0.01, 2.0] , dT ≤ 0.033 ) before fabric sees operator enthusiasm. Caps are not punishment — they preserve fabric existence for next tick.…


**Exercises (11-queen-terminal-py · §12)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 13 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** set AnalogFields.WaveSpeed 2.5
set AnalogFields.TimeScale 2.0
list AnalogFields
grep -i 'CFL\|clamp\|wave' run.log | tail -15
 Expect host to pull effective wave speed below operator request — list readback shows scaled truth. Screenshot of pretty waves without this grep is incomplete testimony.…


**Exercises (11-queen-terminal-py · §13)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 14 — queen-terminal.py — allowlist, dispatch, combinatronic bridge

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (11-queen-terminal-py):** avgBoundaryThermo in ThermoAccountant (binding 2, mirror data_bus[25] ) summarizes mean boundary temperature / entropy density — where fabric meets edges and probes inject. Mouse movement is boundary offense: InjectStrength writes energy at probe positions carried through FieldSocket push constants.…


**Exercises (11-queen-terminal-py · §14)**

1. Run `wiki` in the Field Tech terminal; open `11-queen-terminal-py` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.
