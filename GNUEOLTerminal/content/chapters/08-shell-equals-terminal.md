# Shell ≡ terminal — no hidden second root


## Shell ≡ terminal — no hidden second root

<span class="tag impl">Implemented</span> Focus: **identity**.

Field Technology sells **addressability**: which binding, which guest offset, which jsonl row, which grep line. When those disagree with a pretty UI, the UI is wrong until stderr reconciles.

Old schoolers: you already knew literacy was power. Field Tech names the writable surfaces so dashboards cannot gaslight you.

The terminal is not a toy shell in a browser tab — it is a **Field Tech instrument** wired to KILROY, Queen, plate meld, and optional combinatronic engines.
```python
# queen-field-net.py routes — identical surface
ROUTES = {
    "terminal": "/world/queen-gnu-terminal-embed.html",
    "gnu-terminal": "/world/queen-gnu-terminal-embed.html",
    "shell": "/world/queen-gnu-terminal-embed.html",
}
```


### Section 1 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Flowers and GreenWaves swipes emphasize organic flow and interference patterns on Classic canvas — curriculum shaders where thermo and Phi coupling is visible in ways Big Grin chrome suppresses. Week-one pattern: afternoon on Flowers with inject lab; return to die default for week-two operations.…


**Exercises (08-shell-equals-terminal · §1)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 2 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** ThermoAccountant populates every swipe — canvas-agnostic obligation from Chapter 7. Flowers beauty does not excuse skipping THERMO grep.…


**Exercises (08-shell-equals-terminal · §2)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 3 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** # Swipe Flowers or energy; baseline 120s log segment
set AnalogFields.FieldCoupling 0.1
set AnalogFields.GateFidelity 0.3
set AnalogFields.EntropyFloor 0.01
# Record THERMO tail; then sweep:
set AnalogFields.FieldCoupling 0.9
set AnalogFields.GateFidelity 0.85
set AnalogFields.InjectStrength 4.0
grep -E 'THERMO|entropy|Boundary|prevMaint' run.log | tail -30
 Journal template per sweep row:…


**Exercises (08-shell-equals-terminal · §3)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 4 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Restore defaults after lab — operator ethics. Chapter 4 exam afterward: which layer is ThermoAccountant versus oracle — do not conflate sweep results with Shannon H on zip files.…


**Exercises (08-shell-equals-terminal · §4)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 5 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** RTXPROBE ELLIE category activates with RTX_PROBES=1 — GPU timestamps and invocation counts with zero cost when off (Chapter 7). Use when coupling sweeps cause latency spikes without obvious CFL clamp lines — distinguishes dispatch-bound fabric work from logging volume or accidental host CPU path.…


**Exercises (08-shell-equals-terminal · §5)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 6 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** RTX_PROBES=1 ./linux.sh run 2>&1 | tee /tmp/probe-thermo.log
grep -E 'RTXPROBE|THERMO|CFL' /tmp/probe-thermo.log | tail -25
 RTXProbe does not measure joules — it measures time discipline. Pair with STATUS block GPU ms line for week-three health narrative.…


**Exercises (08-shell-equals-terminal · §6)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 7 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** prevMaintCost (Chapter 4 preview) rises when field state becomes expensive to remember — high GateFidelity , aggressive coupling, resolution jumps. Chapter 3 connects maintenance to thermo pedagogy: Flowers session at fidelity 0.9 should show higher maintenance than baseline 0.3 — not moral judgment, coherence accounting.…


**Exercises (08-shell-equals-terminal · §7)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 8 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Maintenance is why “same looking” frames can cost different entropy — prior frame complexity matters. Clausius spirit: nature taxes irreversibility; engine taxes coherence.…


**Exercises (08-shell-equals-terminal · §8)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 9 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Rudolf Clausius gave operators language: entropy of universe tends upward; heat flows with direction. Field Technology honors spirit via entropy floor seed ~0.015 at clearFieldImages() and minimum noise in diffusion — fabric refuses pretend reversibility.…


**Exercises (08-shell-equals-terminal · §9)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 10 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Irreversibility is not shame. It is the price of time running forward. Clausius named the direction; ThermoAccountant prints the receipt.…


**Exercises (08-shell-equals-terminal · §10)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 11 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Clausius tribute humanizes creditor; EntropyFloor knob and floor seed humanize implementation. Metaphor on proxy — spirit without calorimeter fraud. Chapter 13 Landauer adds erasure floor vocabulary; Chapter 3 adds heat movement vocabulary — same table, different plates (Chapter 4 seating).…


**Exercises (08-shell-equals-terminal · §11)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 12 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Die-default x86.comp evolves bindings 8–10 every dispatch — thermo exists under AmmoOS chrome whether you see heatmap or not. Classic CANVAS.comp swipes exist because human eyes learn coupling faster with color. Product truth on die; pedagogy truth on Classic — compromise this book teaches.…


**Exercises (08-shell-equals-terminal · §12)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 13 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** # Segment A: x86 default 90s mouse move — grep THERMO
# Segment B: swipe GreenWaves 90s same knobs — grep THERMO
# Compare entropyThisFrame distributions — both must be non-silent
 Silence on either segment is failed path — binding 2 or dispatch broken, not “die has no physics.”…


**Exercises (08-shell-equals-terminal · §13)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.


### Section 14 — Shell ≡ terminal — no hidden second root

Old schoolers read source. Young operators read panels. Field Technology demands **both**: stderr from the terminal, JSON from the iron plate, and chapter prose that admits when Grok is impersonating RMS.

> **Field Technology excerpt (08-shell-equals-terminal):** Prompt set AnalogFields.* changes host state; CFL guard may scale before GPU sees values — numerical ethics, not censorship.…


**Exercises (08-shell-equals-terminal · §14)**

1. Run `wiki` in the Field Tech terminal; open `08-shell-equals-terminal` on GitHub Pages.
2. `grep -r gnu_terminal lib/` in a NewLatest checkout.
3. Classify three vendor claims using truth bands (Chapter 27).
4. Record one **LIE of the Year** candidate with attribution class, not a person's name unless historically documented.

**GNU reminder:** Free software existed before SaaS. The terminal is where we prove literacy.
**Impersonation reminder:** Grok-as-RMS voice is parody; grep lines are receipts.
