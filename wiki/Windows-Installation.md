# Windows Installation

Portable / lab use via `stealth.ps1` — not the primary v10.4 Linux field path.

---

## Portable

```powershell
.\stealth.ps1
```

Or use `install/windows/install.ps1` from `install-all.sh` on Windows.

---

## Production field stack

Linux + `install-all.sh` is the supported full install (systemd, nftables, polkit, Tristate).

→ **[Installers](Installers)** · **[Linux Installation](Linux-Installation)**