# Entropy Oracle

Shannon surprise on file samples — flags packed/encrypted payloads. Tunes vigil polling (calm / alert / storm).

- **Module:** `lib/entropy-oracle.sh`
- **Toggle:** `NEXUS_ENTROPY_WATCH=1`
- **Sample:** `NEXUS_ENTROPY_SAMPLE` bytes per check

Does not auto-KILL — raises vigil mode and logs.