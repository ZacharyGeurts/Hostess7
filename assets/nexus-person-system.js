/** NEXUS person system — Pages-safe stub; full lane on loopback panel. */
(function (global) {
  "use strict";
  global.NexusPersonSystem = global.NexusPersonSystem || {
    ready: true,
    pages: true,
    wire: function () {},
  };
})(typeof window !== "undefined" ? window : globalThis);