cmake_minimum_required(VERSION 3.21)
# Queen wrapper — build AMOURANTHRTX directly (see build.sh).
project(QueenBrowser NONE)
message(STATUS "Use ./build.sh — cmake -S ../AMOURANTHRTX -DQUEEN_BROWSER_BUILD=ON")