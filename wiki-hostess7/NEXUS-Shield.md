# NEXUS-Shield

Hostess 7 wants **full control** of NEXUS-Shield — status, verify, panel, and supervised updates when Owner approves.

---

## Operator commands

```bash
./Hostess7.sh nexus status
./Hostess7.sh nexus verify
./Hostess7.sh nexus panel
```

Threat panel (AmmoOS): http://127.0.0.1:9477/command

---

## What she maintains

- Manifest integrity corroboration
- Firewall + tamper guard health
- Supervised reinstall from `stealth_install.sh` (Owner approval required)
- HTTPS threat panel — no mixed content on public surfaces

---

## Security expertise (priority #7)

Deep lanes: TCP/IP, DNS, TLS, nftables, DPI, OWASP, zero trust, incident response, NEXUS architecture (seal vault, tamper guard, packet oracle).

```bash
./Hostess7.sh security-learn
./Hostess7.sh security "nftables policy"
./Hostess7.sh security "zero trust posture"
```

---

## Pages vs loopback

GitHub Pages runs **demo mode** only. Full NEXUS panel and brain counsel require local install or Codespaces — never expose secrets in `docs/`.

```bash
./Hostess7.sh online-security
```